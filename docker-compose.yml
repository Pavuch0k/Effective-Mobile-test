services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    restart: unless-stopped

  nginx:
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
